diff --git a/.github/workflows/docker-drawio-desktop-headless.yaml b/.github/workflows/docker-drawio-desktop-headless.yaml
index 79f0cd3..82a3cf8 100644
--- a/.github/workflows/docker-drawio-desktop-headless.yaml
+++ b/.github/workflows/docker-drawio-desktop-headless.yaml
@@ -15,7 +15,7 @@ permissions:
 jobs:
   build:
     runs-on: ubuntu-latest
-    
+
     # Permissions override for steps that need to write (commit, comment)
     permissions:
       contents: write # For stefanzweifel/git-auto-commit-action
@@ -30,13 +30,14 @@ jobs:
         uses: actions/checkout@v6
         with:
           ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.ref || '' }}
+          fetch-depth: 0
 
       - uses: rlespinasse/github-slug-action@v5
-      
+
       - uses: taiki-e/install-action@v2
         with:
           tool: just
-          
+
       - name: Build Docker Image
         run: just build
         env:
@@ -50,7 +51,7 @@ jobs:
         run: just test-ci
         env:
           DOCKER_IMAGE: ${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}
-        continue-on-error: true 
+        continue-on-error: true
 
       - name: Store Output Logs & Files
         if: steps.test_ci.outcome == 'failure'
@@ -78,7 +79,7 @@ jobs:
             cat patch.diff
             echo "EOF"
           } >> "$GITHUB_OUTPUT"
-        
+
       - name: Comment Patch Diff on PR
         uses: peter-evans/create-or-update-comment@v4
         if: steps.test_ci.outcome == 'failure' && github.event_name == 'pull_request' && steps.patch_diff_step.outputs.diff != ''
@@ -94,14 +95,14 @@ jobs:
             ${{ steps.patch_diff_step.outputs.diff }}
             ```
             </details>
-        continue-on-error: true 
+        continue-on-error: true
 
       - name: Commit and Push Patch (if needed)
         uses: stefanzweifel/git-auto-commit-action@v5
         if: steps.test_ci.outcome == 'failure'
         with:
           commit_message: "fix: apply automated patch after tests failure"
-          branch: ${{ github.ref }} 
+          branch: ${{ github.head_ref }}
 
   build-multiarch:
     runs-on: ubuntu-latest
@@ -134,7 +135,7 @@ jobs:
       - uses: taiki-e/install-action@v2
         with:
           tool: just
-          
+
       - name: Build Docker Image
         run: just build-multiarch
         env:
@@ -148,12 +149,12 @@ jobs:
       id-token: write # Required for OIDC/package registry login
       packages: write # Required to push packages/images to GHCR
       issues: write # If release-that creates issues
-      
+
     needs:
       - build
       - build-multiarch
-      
-    # Concurrency is good here, but cancellation should be disabled so a push 
+
+    # Concurrency is good here, but cancellation should be disabled so a push
     # doesn't cancel a running release.
     concurrency:
       group: ${{ github.workflow }}-${{ github.ref }}-release
